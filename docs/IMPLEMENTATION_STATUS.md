# Implementation Status

Updated after each product completes its gate (`docs/TEST_PLAN.md`). Statuses: **PASS** (all ten
acceptance points met, verified live) / **PARTIAL** (working, real geometry, but formula not
verified against real orders or some acceptance point outstanding) / **NOT CONFIGURED** (no
design definition exists — shown honestly in-app rather than a guessed layout) / **FAIL**.

| Product | Design | Measurement | Components | Formula | Geometry | Views | Dimensions | Validation | PDF | Tests | Status |
|---|---|---|---|---|---|---|---|---|---|---|---|
| Engine (shared) | n/a | n/a | ComponentSpec/DimensionLine types, formula/geometry/dimension/collision/validation/traceability modules + CanonicalSvg renderer + DrawingInspector, all live in `src/engine/` | n/a | n/a | n/a | Collision-tiered, edge-grouped | positive/bounds/geometry-identity checks | screen SVG only (PDF still uses existing `downloadPDF` DOM clone) | Live-verified via browser-automation (0 console errors) | PASS |
| Product-switch state race (fixed 29 Aug 2026) | — | — | `ProductFlow.tsx`'s product `<select onChange>` only called `setSelectedId`; `dims`/`activeView`/addons reset via a `selectedId`-keyed `useEffect`, which runs one render *after* the state change — so for one render, the newly-selected product's resolver ran against the *previous* product's `dims` shape. Harmless when field sets happened to overlap, but produced a real burst of `NaN` SVG attribute errors switching out of Bed once its simplified field set (§ above) stopped overlapping with other products' fields. Fixed by resetting `dims`/`activeView`/`selectedAddons`/`addonDims`/`activeWorkspace` synchronously inside the `onChange` handler itself, in the same update as `setSelectedId` — the `useEffect` stays as a harmless idempotent fallback for other `setSelectedId` callers (e.g. History). | — | — | — | — | — | — | Live-verified: full product-switch sweep (Bed → Side Table → Loft Cabinet → TV Unit → Dining Table → Openable/Sliding Wardrobe) went from 52 console errors to 0 | PASS |
| Bed | **Simplified 29 Aug 2026** per the user's real site-measurement workflow (see docs/BED_SIMPLIFIED_WORKFLOW.md) — replaces the CALC_BED panel/patti/platform breakdown below, which is kept intact but no longer wired into the live product entry for a future "fabrication detail" mode | Exactly 4 fields: Bed Width, Bed Length, Bed Height, Headboard Height (900mm standard default, editable) — no thickness/depth/hydraulic fields | Real: Headboard (Width = Bed Width, auto), Bed body (one plain W×L rectangle, no internal panels), optional LST/RST (Depth/Width entered, Height always auto-fetched from Bed Height, never independently editable) | Direct measurement pass-through — Headboard Width = Bed Width and LST/RST Height = Bed Height are exact equalities, not approximated deductions, so nothing here needs historical-order verification the way CALC_BED's clearance constants did | Real single resolver in `simpleBedGeometry.ts` (`resolveSimpleBedPlan`) | One combined "site sketch" style Plan view only (headboard band above the bed footprint, LST/RST flush at the two head-end corners) — matches the user's own hand-measured reference sketch | Derived from geometry, collision-tiered; title dynamically includes H | Wired (validateMeasurements/Bounds/DimensionIntegrity) | Same `simpleBedGeometry`/`simpleBedCutlist` feeds both screen and PDF — exactly Bed/Headboard/LST/RST rows, no extras | Live-verified against all of the user's own worked examples: 1830×1980mm H=436 base case, LST+RST both attaching at the correct corners with 0 overlap, Bed Width 1830→2000 propagating live to the headboard and width dimension, Bed Height 436→450 and Headboard Height 900→850 tracked independently, PDF downloaded and content-checked row-by-row (4 rows, correct values), 0 console errors | PASS |
| Bed (fabrication detail, retired from live use) | standard (single design) | Real fields (W/L/H/headboardH/D/thk + Include Headboard / Hydraulic toggles) | Real: Headboard, LHS/RHS side panels, Foot Rail, Top Patti, Mattress zone, + Side Table zones. Platform (Top) boards, Back Panel+Front, Bottom and H-Panel shelves are real cutlist rows with no honest single-view placement — fully documented (correct formula, size, and 16mm Top thickness) in the component table and Drawing Inspector, not force-drawn | CALC_BED, verified — D-mapping resolved 29 Aug 2026 (sheet `D`→app `H`, sheet `H`→app `L`, proven against 2 independent source examples; see docs/FORMULA_TRACEABILITY.md); H-Panel fixed-size flag remains open | Real front/plan/side resolvers in `bedGeometry.ts` | Front, Plan, Side all real (not stubs) | Derived from geometry, collision-tiered; every drawn component individually dimensioned | Wired | Component-table PDF mechanism intact | Fully verified as of 29 Aug 2026 (see git history) — kept as working, tested code, just not the live Bed product entry | PASS, but superseded — not reachable from the app UI |
| Side Table | standard | Real fields (W/D/H/drawers) | Real: Top, Skirting, Drawer fronts / Cabinet Body | CALC_SIDE_TABLE, verified | Real front/plan/side resolvers in `sideTableGeometry.ts` | Front, Plan (W×D footprint), Side (D×H profile) all real — Plan/Side added 29 Aug 2026, both dimensioning Depth, which previously appeared on no view at all | Derived from geometry | Wired | Same mechanism | Live-verified standalone: 500×550mm Front, 500×400mm Plan, 400×550mm Side, all three showing Depth where it applies, 0 console errors | PASS |
| Openable Wardrobe | All 25 catalog designs given explicit `WardrobeDesignDef`s (zones + door count + special content) in `wardrobeDesigns.ts` | Existing generic fields (W/H/D/shutters/verticals/shelves/drawers/thk/backThk) + loft/plinth/zone-width fields shown per design's actual needs | Real per-zone: TOP/BOTTOM/SIDE/VERTICAL/doors, + niche/drawer-tower/dressing/loft zone content, zone-based (see wardrobeGeometry.ts) | CALC_OPEN_WARDROBE, verified per zone (zone-splitting is a legitimate real-formula application, not invention — see docs/PRODUCT_STANDARDS.md); door-qty formula carries the source's own flagged "verify with shop floor" caveat | Real front/plan/internal resolvers | Front, Plan, Internal all real | Derived from geometry, collision-tiered | Wired | Same mechanism (unchanged) | Live-verified: all 25 design cards render real distinct previews (0 "NOT CONFIGURED"), selecting cards produces structurally distinct drawings (verified N-door, Mixed Storage 3-zone+loft matching the Rutuja Joshi reference, Niche combo design), 0 console errors | PASS |
| Sliding Wardrobe | Same 25-design system, sliding designs (2/3/4 shutter + combos) | Same as above | Same pattern, sliding-specific deductions | CALC_SLIDE_WARDROBE, verified for the 2-shutter case; 3/4-shutter and combo designs extrapolate the same door-width-split pattern beyond the one verified case — flagged `needsVerification` in the Drawing Inspector, not silently presented as fully verified | Same engine | Same | Same | Wired | Same | Live-verified: "3 Shutter Sliding" (3 real doors) and "Sliding + Dressing Section" (3 doors + real Mirror + Open Shelf zone, 1728mm/672mm zone split summing correctly to 2400mm) — matches the user's own named acceptance example. 0 console errors | PASS |
| Loft (Box family) | — | — | — | CALC_BOX available, not yet wired | — | — | — | — | — | — | Not started |
| Loft Cabinet (Box family) | standard (shared with Cabinet/Storage/Open/Service Box) | Real fields (W/H/D/thk/Number of Boxes/Has Doors) | Real: TOP/BOTTOM/SIDE frame, real vertical dividers forming compartments, real doors (or open compartments) | CALC_BOX, verified exactly against 2 real historical orders (see docs/FORMULA_TRACEABILITY.md); DOOR carries the source's own "not formula-driven, confirmed manual" flag | Real front/plan/side resolvers in `boxGeometry.ts` | Front, Plan, Side all real | Derived from geometry, collision-tiered | Wired | Same mechanism | Live-verified: 6-compartment 3000×400×350mm cabinet — 6 real doors at 494mm each (no overlap), Plan/Side both real, Drawing Inspector trace confirmed correct, 0 console errors | PASS |
| TV Unit | — | — | — | PENDING — no verified formula source | — | — | — | — | — | — | Not started |
| Kitchen | Straight/L/U-shape, existing wall-based wizard | Real 7-step wizard fields (unchanged) | Real: base/wall/loft cabinets, trolley, open box, + a new real `corner` unit (CU-01) at L/U-shape corners | PENDING — no verified formula source (Kitchen's own `defaultConfig.ts` already self-labels every constant "DEMO VALUE") | `ruleEngine.ts` wall-based distribution, now with corner-aware geometry (see below) | Plan, Elevation A, Elevation B all real; Final Drawing sheet (title block + 3 views + materials + checklist) | Derived from geometry | Wired (existing `ValidationIssue[]`/completion%) | `FinalDrawing.tsx`'s "Download SVG" | **Fixed a real, confirmed corner-double-counting bug** (spec's own named issue): Wall A and Wall B cabinet runs both started at the shared corner square, double-counting it as free space on both walls. Added an explicit `corner` module (real ModuleType already in the schema) with real footprint (cabDepth×cabDepth), both wall runs now set back by it. Live-verified: before fix, demo L-shape kitchen showed a false "Wall A: modules exceed wall width by 484mm" error; after fix, correctly shows "2957/3073mm" with no false overflow. **Also fixed**: `LiveDrawing.tsx` (interactive Plan/Elevation A/B workspace) and `FinalDrawing.tsx` (professional fabrication sheet — title block, 3 views, materials schedule, validation checklist) were both fully built but never reachable — `screen === 'drawing'` / `'final'` had no render case in `App.tsx`. Wired both in; both now work end-to-end, 0 console errors, screenshot-confirmed. **Visual polish**: added a `WoodGrainPattern` to `svgPrimitives.tsx`, applied to every base/wall/loft/trolley/open-box cabinet fill across `PlanView.tsx`/`ElevationA.tsx`/`ElevationB.tsx` (Kitchen's own door/drawer pulls, kadappa hatching, and line-weight hierarchy were already well-built — this closes the one visible gap, flat CAD-box fills). | PARTIAL (real corner-aware geometry and a real fabrication sheet now work; formula still honestly Not Verified) |
| Dining Table | — | — | — | PENDING — no verified formula source | — | — | — | — | — | — | Not started |

Out of scope this pass (per user direction): Bedroom, 1BHK, 2BHK, 3BHK composite room layouts;
any product type not already in `PRODUCT_REGISTRY` (Crockery, Vanity, standalone Cabinet/Storage/
Kadappa Rack/Rolling Shutter Box, Custom Furniture builder).
